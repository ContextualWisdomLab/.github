Ah, so `uv pip compile` did work correctly. `pip` got updated to `26.2.1` in `requirements-pip-audit-ci-hashes.txt`.
However, are there any other `requirements*.txt` files that need recompiling?
The project has:
- `requirements-bandit-ci.txt`
- `requirements-opencode-review-ci.txt`
- `requirements-pip-audit-ci.txt`
- `requirements-strix-ci.txt`
- `requirements-strix-ci-overrides.txt`

Let's recompile all of them to be safe and ensure there are no pip vulnerabilities anywhere. Wait, pip is only explicitly brought in `requirements-pip-audit-ci.txt` through `pip-audit`. But wait, does pip-audit bring in pip? Yes, it depends on `pip-api` which depends on `pip`.
Let's see if other files use `pip`.
