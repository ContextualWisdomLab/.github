name: One-shot PR1629 workflow probe

on:
  push:
    branches:
      - fix/no-heuristic-review-admission-current-main

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: Prove job creation
        run: echo workflow-job-started
