name: Temporary Mark PR 1720 Ready

on:
  push:
    branches:
      - fix/noema-review-no-heuristic-timeouts
    paths:
      - .github/workflows/_temp_mark_ready_pr1720.yml

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: temp-mark-ready-pr1720
  cancel-in-progress: true

jobs:
  mark-ready:
    if: github.repository == 'ContextualWisdomLab/.github' && github.actor == 'seonghobae'
    runs-on: ubuntu-slim
    steps:
      - name: Revalidate exact head and mark canonical source-complete PR ready
        env:
          GH_TOKEN: ${{ github.token }}
          EXPECTED_HEAD: ${{ github.sha }}
        shell: bash
        run: |
          set -euo pipefail
          pr_json="$(gh pr view 1720 --repo ContextualWisdomLab/.github --json state,isDraft,headRefOid)"
          test "$(jq -r '.state' <<<"$pr_json")" = "OPEN"
          test "$(jq -r '.isDraft' <<<"$pr_json")" = "true"
          test "$(jq -r '.headRefOid' <<<"$pr_json")" = "$EXPECTED_HEAD"
          gh pr ready 1720 --repo ContextualWisdomLab/.github
