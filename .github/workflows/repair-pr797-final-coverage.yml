name: PR 797 repair retrigger marker

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert-marker:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "This marker is deleted by the exact-head repair workflow."
