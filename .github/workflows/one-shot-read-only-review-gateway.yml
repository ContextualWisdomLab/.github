name: One-shot read-only review gateway migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Migration staging is paused while verified inputs are materialized."
