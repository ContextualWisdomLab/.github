strix-agent==1.0.4
google-cloud-aiplatform==1.133.0
protobuf<7.0.0
cryptography==50.0.0
python-multipart==0.0.32
pyasn1==0.6.4
# Transitive via strix-agent. 3.14.3 clears GHSA-cq5v-8q36-5273 (HIGH, OOB heap
# read in the C HTTP response parser), GHSA-mq44-7p77-q5h7 (unnegotiated
# permessage-deflate frames accepted) and GHSA-mfx4-hv73-q22v (request smuggling
# via WebSocket upgrade). Floor, not a pin, so Dependabot can keep moving it.
aiohttp>=3.14.3
