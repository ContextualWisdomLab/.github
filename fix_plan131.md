Ah! `security-scan.yml` ALSO has top-level `permissions:` AND job-level permissions!
I should just change the top-level `permissions:` to `permissions: {}` or `permissions: read-all` in both `osv-scanner-pr.yml` and `security-scan.yml`.
What about `strix.yml`?
