FROM scratch
USER 65532:65532
HEALTHCHECK NONE

# ClusterFuzzLite discovery marker for OpenSSF Scorecard. Python harnesses live
# in fuzz/ so the central workflow repository has an explicit fuzzing surface.
